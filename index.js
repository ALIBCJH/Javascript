const age = 18;
 const votingAge = (age >= 18) ? "You are eligible to vote": "You are not eligble to Vote";
 console.log(votingAge);

 const A = 23;
 const b = 23;
 const C = 45;

 const addResult = (A, B, C) => {
    return A + B + C;
 }
 console.log(addResult);